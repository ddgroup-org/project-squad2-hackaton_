# Log de execução de demandas

Uma linha por demanda executada via `/executar-demanda`. Ver [README.md](README.md) e [docs/como-executar-demandas.md](../docs/como-executar-demandas.md).

| # | Data | Demanda (resumo) | O que foi feito | Commit |
| --- | --- | --- | --- | --- |
| 01 | 2026-08-13 | Solution Design + Data Mapping da Cromatta Química — PDF corporativo (`02_Solution_Design_Cromatta_Quimica_Squad02.pdf`), com logo e cores da marca (`imgs/`) | Gerado via HTML+CSS/SVG renderizado a PDF por Chrome headless (`entregaveis/`). Conteúdo: diagrama de objetos, Data Mapping campo a campo, Security Model, automações previstas, uso do Claude por etapa. Validado via MCP (`run_soql_query` em `PermissionSet`/`EntityDefinition`) que a "Tarefa 02" (Security Model) e o objeto `Amostra__c` ainda não existem na org — documentado como design especificado, não implementado. Fechada a decisão pendente em `architecture.md`: `Amostra__c` é objeto customizado (Master-Detail em Opportunity). **Correção posterior (ver linha 03):** a relação com Opportunity foi corrigida para Lookup, conforme o BRD oficial. | d87e560 |
| 02 | 2026-08-13 | **Reconciliação retroativa** — Permission Sets `Vendedor` e `Laboratorio` já existiam na org (criados às 14:28 e 14:33 UTC), fora do fluxo `/executar-demanda`, sem nenhum registro prévio aqui | Reanálise arquitetural (agente `salesforce-architect`) encontrou os dois Permission Sets na org, criados por **Ricardo Custodio** (mentor do hackathon, não membro do Squad 02) — descrição interna citava "demanda-02" mas não havia entrada correspondente neste log nem metadata em `force-app/`. Feito retrieve direcionado via MCP (`retrieve_metadata`) trazendo `Vendedor.permissionset-meta.xml` e `Laboratorio.permissionset-meta.xml` para `force-app/main/default/permissionsets/`. Decisão de reconciliar (não descartar) registrada em [ADR 0004](../decisions/0004-reconciliacao-permissionsets-fora-do-fluxo.md). Nenhum usuário tem os Permission Sets atribuídos ainda; falta o Permission Set `Administrador Comercial` e a Queue "Laboratório". | (pendente — ver commit desta entrega) |
| 03 | 2026-08-13 | Reconciliação do modelo de dados com o BRD oficial (`entregaveis/BRD_Cromatta_Quimica_Squad02_final.pdf`, v1.0) | BRD trouxe duas divergências em relação ao que havia sido decidido antes: (1) Account deve ser objeto único com `TipoPessoa__c`, sem Record Type — [ADR 0003](../decisions/0003-account-sem-record-type-tipopessoa.md), substitui a ADR 0001 (Person Accounts confirmado habilitado na org via a mesma reanálise, mas não usado pelo modelo); (2) `Amostra__c` deve ser Lookup em Opportunity, não Master-Detail. `architecture.md` e `business-scenario.md` atualizados (nomenclatura de campos, threshold de 40% de concentração de receita, SLA de 10/5 dias, químicos Sérgio e André confirmados, permission set "Administrador Comercial" em vez de "Gestor"). Decisões confirmadas com o tech lead antes de aplicar. | (pendente — ver commit desta entrega) |
